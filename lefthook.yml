pre-commit:
  parallel: true
  jobs:
    - name: biome lint & format
      run: bun x biome check --write {staged_files}
      glob: "*/*.{js,ts,jsx,tsx,json}"
    - name: type check
      run: bun check-types
      glob: "*.{ts,tsx}"
